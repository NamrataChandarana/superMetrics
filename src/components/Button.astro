---
const {title ,style, icon} = Astro.props
---


<button class=` rounded-xl bg-primary-btn-blue  px-[16px] border border-solid-lightBlue btn-outer-dropShadow  btn-inner-dropShadow ${style}`>
    <p class="text-white font-semibold">{title}</p>
    {icon && <span>{<icon/>}</span>}
</button>